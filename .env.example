# Opsional: identitas app yang dikirim ke OpenRouter via header
# HTTP-Referer / X-Title (default: http://localhost:3000 + "ChatBot Web BYOK").
# Isi saat deploy production agar sesuai domain asli.
OPENROUTER_REFERER=https://domain-kamu.com
OPENROUTER_TITLE="ChatBot Web BYOK"
